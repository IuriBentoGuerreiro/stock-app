import { SaleItemsFormatPipe } from './sale-items-format.pipe';

describe('SaleItemsFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SaleItemsFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
